# 사용자 웹 작업 진행 기록

작성일: 2026-05-02

이 문서는 `SMART_WEB` 사용자 웹에서 지금까지 진행한 디자인 및 화면 구성 작업을 정리한 기록이다.

## 1. 작업 목표

`SMART_WEB`은 Harvest Slot 사용자 예약 웹을 Flutter Web으로 구현하는 프로젝트다.

이번 작업의 중심 목표는 아래와 같다.

- 사용자 웹 주요 예약 흐름 화면 구성
- 프로토타입 기준 디자인 반영
- 사용자 입장에서 자연스러운 화면 문구 정리
- 반응형 웹 화면 구조 정리
- 화면 상단 브랜드 AppBar 통일
- 추후 백엔드 연결 전 더미 데이터 기반 화면 흐름 완성

## 2. 참고한 주요 자료

- `smart_docs/01_web_prototype/prototype_user_web_m3/index.html`
- `smart_docs/01_web_prototype/prototype_user_web_m3/pages/product-detail.html`
- `smart_docs/01_web_prototype/prototype_user_web_m3/pages/checkout.html`
- `smart_docs/01_web_prototype/prototype_user_web_m3/pages/return-request.html`
- `smart_docs/00_harvest_slot_docs_v3_2` 기획서 및 화면 정의 문서
- `smart_web/docs/ksm/사용자웹_필요정보_정리.md`
- `smart_web/docs/ksm/사용자웹_추가기능_제안.md`

## 3. 전체 구조

Flutter Web 기준으로 MVVM 구조를 유지했다.

```text
lib/
  app/
  core/
  data/
    models/
    repositories/
  presentation/
    view_models/
    views/
    widgets/
```

화면은 `presentation/views`, 화면 상태는 `presentation/view_models`, 더미 데이터와 모델은 `data` 아래에서 관리한다.

## 4. 구현 및 수정한 주요 화면

### 홈 화면

- 프로토타입의 고객 예약 흐름 전체 시안을 기준으로 재구성했다.
- 상단 브랜드, 상품, 내 주문, 회원가입, 로그인, 예약함 메뉴를 배치했다.
- 반응형에서 메뉴가 깨지지 않도록 AppBar 구조를 정리했다.
- 대표 상품 영역과 수확 예정 날짜 표시를 반영했다.
- `Harvest Slot` 브랜드 클릭 시 홈으로 이동하도록 했다.

### 상품 목록 화면

- 고객용 문구로 정리했다.
- 상품 목록 상단을 `Harvest Slot` 브랜드 링크 형태로 변경했다.
- 상품 카드에서 불필요한 `5kg 박스` 표시를 정리했다.
- 관리자용 느낌이 강한 문구를 고객용 안내 문구로 바꿨다.

### 상품 상세 화면

- `product-detail.html` 프로토타입을 기준으로 화면을 재구성했다.
- 상품 이미지, 상품 설명, 패키지 단위, 수확 슬롯 선택, 박스 수량 선택, 예약함 담기 흐름을 구성했다.
- 왼쪽 빈 영역에는 상품 설명과 수확 직배송 안내 카드 성격의 콘텐츠를 추가했다.
- 상단 AppBar를 브랜드 AppBar로 통일했다.

### 담아둔 수확 상품 화면

- 로컬 예약함 화면을 구성했다.
- 담긴 상품을 확인하고, 예약 전 확인으로 넘어갈 수 있게 했다.
- 담긴 상품을 제거할 수 있는 버튼과 안내 문구를 추가했다.
- 상단 AppBar를 브랜드 AppBar로 통일했다.

### 예약 전 확인 화면

- 예약 생성 전 상품, 수량, 가격을 확인하는 화면으로 구성했다.
- 고객이 실제 이용하는 흐름에 맞게 문구를 정리했다.
- 수확 일정 변동 가능성 안내를 유지했다.
- 상단 AppBar를 브랜드 AppBar로 통일했다.

### 주문서 작성 화면

- `checkout.html` 프로토타입을 기준으로 디자인을 재구성했다.
- 배송지, 배송 메모, 주문 요약, 결제 진행 버튼 영역을 구성했다.
- 기본 배송지 선택처럼 보이는 카드 UI를 반영했다.
- 상단 AppBar를 브랜드 AppBar로 통일했다.

### 결제 화면

- 고객 화면에 `Mock 결제`처럼 개발용 문구가 보이지 않도록 정리했다.
- 결제 대기, 결제 금액, 결제 완료 흐름이 자연스럽게 보이도록 수정했다.
- 상단 AppBar를 브랜드 AppBar로 통일했다.

### 주문 완료 화면

- 주문 완료 후 고객에게 보여줄 완료 카드 화면을 구성했다.
- 주문번호, 결제 금액, 농가 확인 중 상태를 안내한다.
- 홈으로 이동, 주문 상세 보기 버튼을 제공한다.
- 상단 AppBar를 브랜드 AppBar로 통일했다.

### 내 주문 화면

- 주문 목록과 주문 상태를 확인하는 화면이다.
- 주문 상세로 이동할 수 있게 유지했다.
- 상단 AppBar를 브랜드 AppBar로 통일했다.

### 주문 상세 화면

- 주문 진행 상태 영역을 왼쪽 기준으로 정렬했다.
- 상태 타임라인을 고객이 보기 쉬운 `진행 현황` 형태로 변경했다.
- 중복으로 보이던 배송사 문구를 자연스러운 배송 진행 문구로 바꿨다.
- 반품 신청 버튼과 주문 요약 영역을 유지했다.
- 상단 AppBar를 브랜드 AppBar로 통일했다.

### 반품 신청 화면

- `return-request.html` 프로토타입을 기준으로 다시 구성했다.
- 반품할 상품 선택, 반품 사유 카드, 상세 사유, 증빙 이미지, 요청 금액, 반품 요청 요약을 구성했다.
- 고객이 실제 이용하는 문구로 전체 텍스트를 정리했다.
- 상단 AppBar를 브랜드 AppBar로 통일했다.

### 반품 요청 완료 화면

- 반품 요청 후 바로 홈으로 이동하지 않고 확인 페이지로 이동하도록 새 화면을 추가했다.
- 반품 요청 접수 완료, 주문번호, 농가 확인 후 안내 상태를 보여준다.
- 홈으로 이동, 내 주문 보기 버튼을 제공한다.
- 상단 AppBar를 브랜드 AppBar로 통일했다.

### 로그인, 회원가입, 이메일 인증 화면

- `login.html`, `signup.html`, `email-verify.html` 프로토타입을 기준으로 다시 구성했다.
- 왼쪽은 이미지 패널, 오른쪽은 흰색 카드 폼으로 구성했다.
- 외부 이미지 로딩 실패 시 초록 그라데이션 fallback이 보이도록 했다.
- 로그인, 회원가입, 이메일 인증 화면 모두 상단 AppBar를 `Harvest Slot` 브랜드 링크로 변경했다.
- 이메일 인증 코드 박스는 한 줄로 보이도록 크기와 배치를 조정했다.

## 5. 공통 작업

### 브랜드 AppBar 공통화

새 공통 위젯을 추가했다.

```text
lib/presentation/widgets/brand_app_bar_title.dart
```

적용된 화면에서 `Harvest Slot` 브랜드를 클릭하면 홈으로 이동한다.

적용 화면:

- 상품 상세
- 담아둔 수확 상품
- 예약 전 확인
- 주문서 작성
- 결제
- 주문 완료
- 내 주문
- 주문 상세
- 반품 신청
- 반품 요청 완료
- 로그인
- 회원가입
- 이메일 인증

### 한글 문구 정리

- 깨진 한글 문구를 정상 한글로 복구했다.
- 고객 화면에 개발자용 문구가 보이지 않도록 수정했다.
- `Mock`, API, DB, 내부 상태 코드성 표현은 고객 화면에서 제거했다.

### 반응형 처리

- 홈 화면의 상단 메뉴가 화면 크기에 따라 깨지지 않도록 수정했다.
- 모바일에서 상품 카드가 overflow 나지 않도록 조정했다.
- 이메일 인증 코드 박스가 한 줄로 보이도록 조정했다.

## 6. 현재 검증 상태

주요 수정 후 아래 명령으로 확인했다.

```bash
dart format
flutter analyze
```

현재 마지막 확인 기준으로 `flutter analyze`는 통과했다.

## 7. 현재 실행 관련 메모

Flutter Web 서버는 작업 중 주로 아래 포트를 사용했다.

```text
http://localhost:6197
http://localhost:5851
```

화면 수정 후 변경 사항이 바로 안 보이면 브라우저에서 `Ctrl + F5`로 강력 새로고침이 필요할 수 있다.

서버가 이전 빌드 결과를 잡고 있으면 Flutter Web 서버를 재시작해야 한다.

## 2026-05-06 최신 작업 추가 기록

아래 작업을 추가로 진행했다.

- 상단바에 `마이페이지` 메뉴를 항상 표시하도록 변경했다.
- 비로그인 상태에서 마이페이지 접근 시 로그인 안내 후 로그인 화면으로 이동하도록 했다.
- 모바일 상품 카드와 홈 상품 카드의 세로 높이를 줄였다.
- 홈 상품 카드 클릭 시 상품 목록이 아니라 해당 상품 상세 페이지로 바로 이동하도록 변경했다.
- 예약함, 예약 확인, 주문서 작성, 결제, 주문 완료, 주문 상세, 반품 신청, 반품 완료 화면에 진행 상태 배지를 추가했다.
- 마이페이지 상단 요약 카드를 추가했다.
- 마이페이지에서 중복 기본 배송지 패널을 제거하고, 배송지 관리는 별도 페이지로 분리했다.
- 배송지 관리, 배송지 추가, 배송지 수정 화면을 추가했다.
- 배송지 추가/수정 화면에 필수 입력 검증을 추가했다.
- 배송 요청사항에 `직접 입력` 옵션을 추가했다.
- 회원 정보 보기 화면과 회원 정보 수정 화면을 추가했다.
- 이메일 인증 상태는 기본 정보의 이메일 줄에 `인증 완료` 배지로 표시하도록 정리했다.
- 마이페이지, 배송지, 회원 정보 API 연결 지점을 `마이페이지_배송지_회원정보_API연결정리.md`에 정리했다.
- 결제 화면을 수확 예약 상품, 배송 정보, 결제 수단, 결제 금액, 확인 동의 구조로 재구성했다.
- 주문 완료 화면을 결제 후 농가 확인 흐름에 맞게 정리했다.
- 주문 상세 화면의 더미 상태와 문구를 `농가 확인 중` 기준으로 통일했다.
- 반품 신청 버튼은 배송 완료 상태에서만 보이도록 정리했다.

상세 기록은 아래 문서를 참고한다.

```text
docs/ksm/2026-05-06_최근_작업_진행_기록.md
docs/ksm/마이페이지_배송지_회원정보_API연결정리.md
```
