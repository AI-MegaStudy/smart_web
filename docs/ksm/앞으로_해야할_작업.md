# 사용자 웹 앞으로 해야 할 작업

작성일: 2026-05-02  
최신화: 2026-05-10

이 문서는 `SMART_WEB` 사용자 웹에서 앞으로 진행해야 할 작업을 최신 상태 기준으로 정리한다.

## 0. 2026-05-10 현재 상태

실제 API 연결 기준으로 아래 작업은 완료 또는 확인 완료 상태다.

- 상품 목록, 상품 상세, 수확 슬롯 API 연결
- 로그인, 토큰 저장, 새로고침 후 로그인 유지
- 마이페이지 `/me` 연결
- 회원정보 수정 `PUT /api/v1/me` 연결
- 배송지 관리 `/api/v1/me/addresses` 계열 API 연결
- 이메일 인증 DB 보정 반영 및 백엔드 재연결
- 이메일 인증 메일 발송, 코드 검증, 회원가입, 로그인 확인
- 예약 미리보기, 예약 생성, 주문 생성, 결제 승인 연결
- 주문 목록, 주문 상세, 결제 정보 표시 연결
- 반품 목록 빈 상태와 반품 요청 API 연결
- 로그인/회원가입 기본 테스트 값 제거
- 고객 화면의 개발자용, 시연용 문구 제거
- 알 수 없는 서버 오류를 고객용 안내 문구로 변환

현재 보류:

- 새 계정 전체 클릭 QA: 이메일 인증과 로그인은 확인 완료, 예약함부터 주문 상세까지 최종 시연 흐름 재확인 필요
- 배송/반품 최종 성공 QA: 농가/관리자 앱에서 배송 상태 변경 흐름이 연결된 뒤 확인 필요

## 1. 현재 완료된 주요 작업

아래 항목은 화면 구현 기준으로 완료된 상태다.

- 홈 화면
- 상품 목록 화면
- 상품 상세 화면
- 예약함 화면
- 예약 전 확인 화면
- 주문서 작성 화면
- 결제 화면
- 주문 완료 화면
- 내 주문 화면
- 주문 상세 화면
- 반품 신청 화면
- 반품 요청 완료 화면
- 로그인 화면
- 회원가입 화면
- 이메일 인증 화면
- 마이페이지 화면
- 배송지 관리 화면
- 배송지 추가 화면
- 배송지 수정 화면
- 회원 정보 보기 화면
- 회원 정보 수정 화면

## 2. 최근 완료된 보강 작업

2026-05-06부터 2026-05-08까지 아래 작업을 완료했다.

- 상단바에 `마이페이지` 메뉴를 항상 보이도록 추가
- 비로그인 상태에서 마이페이지 접근 시 로그인 필요 안내 처리
- 모바일 상품 카드 높이와 내부 여백 축소
- 홈 상품 카드 클릭 시 해당 상품 상세 페이지로 바로 이동
- 예약/주문/반품 흐름 페이지 상단에 현재 진행 상태 배지 추가
- 마이페이지 상단 요약 카드 추가
- 마이페이지 하단의 중복 기본 배송지 카드 제거
- 배송지 관리/추가/수정 화면 추가
- 배송지 추가/수정 화면 필수 입력 검증 추가
- 배송 요청사항 `직접 입력` 옵션 추가
- 회원 정보 보기/수정 화면 추가
- 이메일 인증 상태를 기본 정보 안의 `인증 완료` 배지로 표시
- 마이페이지, 배송지, 회원 정보 API 연결 지점 문서화
- 결제 화면을 수확 예약 서비스에 맞는 `결제 전 확인` 구조로 재구성
- 주문 완료/주문 상세 화면을 결제 후 `농가 확인 중` 흐름에 맞게 정리
- 팀장 컨펌 체크리스트 기준으로 로그인/로그아웃 헤더 노출을 확인
- 비로그인 예약 진입 시 로그인 안내 흐름 확인
- 예약 확인 페이지의 잔여 수량 재검증과 주문서 작성 비활성화 흐름 확인
- 주문서 작성 화면에 배송지 선택 카드와 자동 채움 흐름 추가
- 배송지 별명은 선택 입력값으로 정리
- 결제 수단 드롭다운과 준비중 팝업 추가
- 예약 확인 요약 카드의 안내 문구를 역할별 강조 박스로 분리
- 홈 패럴랙스 섹션의 버튼형 배지를 캡션 라인형으로 변경
- 홈 `이번 주 오픈 수확 슬롯`은 대표 3개 노출 정책으로 확인
- 사과 카테고리를 `신고`, `부사` 두 가지로 정리
- 고객센터 전화번호 클릭 연결과 이메일 문의 링크 추가

## 3. 앞으로 우선 진행할 작업

| 우선순위 | 작업 | 목적 |
|---|---|---|
| 1 | 새 계정 전체 QA | 로그인 후 예약, 주문, 결제, 주문 상세까지 한 번에 확인 |
| 2 | 고객 화면 문구 점검 | 최종 클릭 QA 중 어색한 안내 문구, 버튼 문구, 상태 문구 확인 |
| 3 | 배송/반품 최종 QA | 농가/관리자 앱 배송 상태 변경 후 배송 조회와 반품 신청 확인 |
| 4 | 백엔드 전달 문서 공유 | 임시 수정 복구, 추가 API, 남은 QA 항목을 백엔드 담당자와 팀장님에게 전달 |
| 5 | 최종 발표/시연 QA | 데스크톱/모바일 시연 흐름 점검 |

## 4. 더미 데이터 최종 통일

대부분 실제 API 연결로 전환되었지만, 발표 전 아래 표시는 다시 확인한다.

정리할 항목:

- 상품명
- 농장명
- 수확 예정 날짜
- 패키지 단위
- 잔여 수량
- 주문번호
- 주문 상태
- 배송 상태
- 결제 금액
- 반품 가능 주문 상태
- 회원 이름, 이메일, 휴대폰번호
- 기본 배송지

권장 기준:

```text
대표 주문번호: ORD-20261012-008
대표 상품: 신고 사과
대표 농장: 청송 햇살농원
대표 수확 예정: 10.12-10.18
대표 회원: 홍길동
대표 이메일: minji.kim@harvest-slot.test
대표 연락처: 010-1111-2222
대표 배송지: 서울시 강남구 테헤란로 123
대표 택배사: CJ대한통운
대표 송장번호: 5891-1202-4810
결제 직후 상태: 농가 확인 중
```

## 5. 빈 상태/오류 상태 보강

현재는 대부분 더미 데이터가 있는 상태를 기준으로 화면이 구성되어 있다.

추가하면 좋은 상태:

- 예약함이 비어 있을 때
- 주문 목록이 비어 있을 때
- 배송지 목록이 비어 있을 때
- 상품 목록이 비어 있을 때
- 상품 이미지 로딩 실패
- API 로딩 실패
- 네트워크 오류

고객 화면에는 API 경로, DB명, 내부 상태 코드가 노출되지 않도록 한다.

## 6. 백엔드 연결 준비

백엔드 담당자에게 전달해야 할 주요 연결 지점은 아래와 같다.

### 인증/회원

- `POST /auth/customers/signup`
- `POST /auth/email/send`
- `POST /auth/email/resend`
- `POST /auth/email/verify`
- `GET /auth/email/status`
- `POST /auth/login`
- `GET /me`
- `PUT /me`

### 마이페이지

- `GET /me`

필요 데이터:

- 이름
- 이메일
- 휴대폰번호
- 이메일 인증 여부
- 진행 중 주문 수
- 최근 주문 상태
- 기본 배송지 요약

자세한 내용은 `docs/ksm/마이페이지_배송지_회원정보_API연결정리.md`를 기준으로 한다.

### 배송지

- `GET /me/addresses`
- `POST /me/addresses`
- `PUT /me/addresses/{address_id}`
- `PATCH /me/addresses/{address_id}/default`

### 상품

- `GET /products?featured=true`
- `GET /products?featured=true&limit=3`
- `GET /products`
- `GET /products/{product_id}`
- `GET /products/{product_id}/slots`
- 상품 카테고리 또는 품종 값은 MVP 기준 `신고`, `부사`를 우선 사용한다.

### 예약

- `POST /reservations/preview`
- `POST /reservations`

### 주문/결제

- `POST /orders/from-reservation`
- `POST /payments/mock-approve`
- `GET /me/orders`
- `GET /me/orders/{order_id}`
- `GET /me/orders/{order_id}/shipment`

### 반품

- `POST /returns`

### 고객센터/푸터

MVP에서는 프론트 고정값으로 표시 가능하다.

운영 정보가 바뀔 수 있으면 아래 필드를 설정 API 또는 관리자 관리값으로 분리한다.

- `support_phone`
- `support_email`
- `support_hours`
- `terms_url`
- `privacy_url`

## 7. 실제 API 연동 전 준비

실제 API 연동 전 아래 구조를 정리하면 좋다.

- 더미 Repository와 실제 API Repository 분리
- 공통 API 클라이언트 정리
- 인증 토큰 저장 위치 정리
- API 오류 메시지 고객용 문구로 변환
- 로딩/성공/실패 상태 ViewModel 기준 통일
- 라우트 arguments 타입 정리

### Repository 교체 기준

2026-05-08 기준으로 ViewModel은 아래 Repository 인터페이스를 바라보도록 정리했다.

```text
ProductRepositoryContract
LocalBasketRepositoryContract
OrderRepositoryContract
```

현재 구현체:

| 현재 구현체 | 역할 | 실제 API 연결 시 교체 방향 |
|---|---|---|
| `ProductRepository` | 홈 대표 상품, 상품 목록, 상품 상세, 수확 슬롯 더미 데이터 제공 | `ApiProductRepository`로 교체 |
| `LocalBasketRepository` | 예약 생성 전 로컬 예약함 상태 관리 | 브라우저 저장소 기반 Repository로 유지하거나 `LocalStorageBasketRepository`로 분리 |
| `OrderRepository` | 주문 목록, 주문 상세, 반품 요청 더미 상태 제공 | `ApiOrderRepository`와 `ApiReturnRepository`로 분리 가능 |

ViewModel 연결 기준:

| ViewModel | 사용하는 인터페이스 |
|---|---|
| `HomeViewModel` | `ProductRepositoryContract` |
| `ProductListViewModel` | `ProductRepositoryContract` |
| `ProductDetailViewModel` | `ProductRepositoryContract`, `LocalBasketRepositoryContract` |
| `LocalBasketViewModel` | `LocalBasketRepositoryContract` |
| `ReservationConfirmViewModel` | `LocalBasketRepositoryContract`, `ProductRepositoryContract` |
| `CheckoutViewModel` | `LocalBasketRepositoryContract` |
| `PaymentViewModel` | `LocalBasketRepositoryContract` |
| `OrderCompleteViewModel` | `LocalBasketRepositoryContract` |
| `MyOrdersViewModel` | `OrderRepositoryContract` |
| `OrderDetailViewModel` | `OrderRepositoryContract` |
| `ReturnRequestViewModel` | `OrderRepositoryContract` |

실제 API 연결 시 권장 순서:

1. `core/api`에 공통 API 클라이언트를 만든다.
2. `ApiProductRepository`, `ApiOrderRepository` 같은 실제 구현체를 추가한다.
3. 앱 시작 지점 또는 간단한 Repository provider에서 ViewModel에 실제 구현체를 주입한다.
4. 서버 오류는 ViewModel에서 고객용 문구로 변환해 화면에 표시한다.
5. 서버 상태 코드는 `OrderStatusMapper`를 통해 고객용 한글 문구로 변환한다.

## 8. 실제 API 연결 우선순위

프론트 화면 완성 후 실제 API를 붙일 때는 아래 순서가 가장 안정적이다.

### 1순위. 상품과 수확 슬롯

상태: 2026-05-09 기준 1차 연결 진행 완료

먼저 연결할 API:

- `GET /products?featured=true&limit=3`
- `GET /products`
- `GET /products/{product_id}`
- `GET /products/{product_id}/slots`

이유:

- 로그인 없이도 확인 가능한 공개 데이터다.
- 홈, 상품 목록, 상품 상세가 모두 이 데이터에 의존한다.
- 상품/슬롯 응답 구조가 맞아야 예약함, 예약 확인, 주문 흐름을 안정적으로 붙일 수 있다.
- MVP 사과 카테고리는 `신고`, `부사` 기준으로 맞춘다.

프론트에서 확인할 것:

- 상품 목록 필터가 `신고`, `부사` 기준으로 정리되는지
- 상품 상세의 패키지 단위와 수확 슬롯이 맞게 표시되는지
- 잔여 수량과 가격이 슬롯 기준으로 표시되는지
- 상품명은 백엔드 `product_name` 원문을 그대로 표시하는지
- 상품 상세 예약 가능 여부는 상세 응답의 `open_slot_count`만 보지 않고 슬롯 조회 결과를 기준으로 판단하는지
- 슬롯이 없는 상품은 예약 불가가 아니라 `수확 일정 준비중` 안내로 보이는지

2026-05-09 확인 내용:

- 공통 `ApiClient`와 `ProductApiRepository`를 추가했다.
- 홈, 상품 목록, 상품 상세, 예약 확인 ViewModel이 실제 상품 Repository를 사용할 수 있게 연결했다.
- 상품 카드와 상세 화면에서 실제 슬롯 조회 결과를 기준으로 수확 예정일, 잔여 수량, 가격을 표시하도록 정리했다.
- `flutter analyze` 통과를 확인했다.

남은 확인:

- 백엔드 더미 데이터의 상품명, 품종명, 이미지 URL 최종 정리
- `GET /products?featured=true`의 대표 상품 개수 정책 확인
- 실제 운영 데이터에서 `신고`, `부사`만 내려오는지 확인

### 2순위. 인증과 로그인 세션

2026-05-10 현재 상태:

- 로그인 API 연결 완료
- 토큰 저장 및 새로고침 후 로그인 유지 확인 완료
- `/me` 조회 연결 완료
- 이메일 인증은 DB에서 `attempt_count`, `updated_at`, `account_id NULL 허용`, `verification_code VARCHAR(255)` 보정 완료
- 백엔드 환경변수는 프로젝트 루트 `.env` 기준으로 정리
- 새 계정 기준 이메일 발송, 코드 검증, 회원가입, 로그인 확인 완료

먼저 연결할 API:

- `POST /auth/customers/signup`
- `POST /auth/email/send`
- `POST /auth/email/verify`
- `POST /auth/email/resend`
- `GET /auth/email/status`
- `POST /auth/login`
- `GET /me`

이유:

- 예약 생성, 주문, 마이페이지, 반품은 로그인 상태가 필요하다.
- 로그인 상태는 실제 토큰 기반 저장으로 전환했다.

프론트에서 확인할 것:

- 로그인 성공 시 토큰 저장 위치
- 새로고침 후 로그인 유지 여부
- 로그아웃 시 토큰 삭제
- 이메일 인증 상태 표시

2026-05-10 확인 내용:

- 이메일 인증 메일 발송 성공
- 6자리 인증 코드 검증 성공
- 이메일 인증 후 고객 회원가입 성공
- 로그인 성공
- 고객 화면 문구 검색에서 `Mock`, `더미`, `API`, `DB`, `테스트 코드`, `database integrity error`, `시연`, `백엔드`, `팀장`, `연결 예정` 직접 노출 문자열은 발견되지 않음

### 3순위. 마이페이지와 배송지

2026-05-10 현재 상태:

- `GET /me`, `PUT /me` 연결 완료
- `/me/addresses` 계열 API를 MVP 방식으로 추가하고 연결 완료
- 주문서 작성 화면의 기본 배송지 반영 확인 완료

먼저 연결할 API:

- `GET /me`
- `PUT /me`
- `GET /me/addresses`
- `POST /me/addresses`
- `PUT /me/addresses/{address_id}`
- `PATCH /me/addresses/{address_id}/default`

이유:

- 주문서 작성 화면에서 배송지 선택과 자동 채움이 이미 구현되어 있다.
- 배송지 API는 추가 및 연결을 완료했다.
- 카카오 주소 검색은 프론트에서 주소 값을 채우고, 백엔드는 저장값만 받으면 된다.

프론트에서 확인할 것:

- 기본 배송지 선택
- 최근 사용 배송지 표시
- 배송지 별명은 선택값으로 처리
- 상세주소 미입력/입력 흐름

### 4순위. 예약과 주문 생성

먼저 연결할 API:

- `POST /reservations/preview`
- `POST /reservations`
- `POST /orders/from-reservation`

이유:

- 예약 가능 수량, 가격, 슬롯 상태 검증은 서버가 최종 판단해야 한다.
- 로컬 예약함은 서버 저장 전 단계이므로, 예약 생성 성공 시점부터 서버 데이터로 넘어가야 한다.

프론트에서 확인할 것:

- 서버가 내려준 예약 가능 여부 메시지
- 수량 부족, 마감 슬롯, 가격 변경 오류 처리
- 예약 만료 시간이 있다면 주문서 작성 화면에 표시 가능

### 5순위. 결제와 주문 조회

먼저 연결할 API:

- `POST /payments/mock-approve`
- `GET /me/orders`
- `GET /me/orders/{order_id}`
- `GET /me/orders/{order_id}/shipment`

이유:

- 결제 승인 후 주문 상태가 `PROCUREMENT_REQUESTED`로 바뀌어야 한다.
- 내 주문과 주문 상세는 상태 매핑, 배송 정보, 반품 버튼 노출 기준을 검증하는 핵심 화면이다.

프론트에서 확인할 것:

- 결제 성공 후 주문 완료 화면 이동
- 주문 목록의 상태 문구
- 주문 상세 타임라인
- 배송 중 또는 배송 완료 상태의 택배사/송장번호 표시

### 6순위. 반품

먼저 연결할 API:

- `POST /returns`
- `GET /me/returns`

이유:

- 반품은 주문과 배송 상태가 먼저 연결된 뒤 붙이는 것이 안전하다.
- 현재 프론트 기준은 배송 완료 상태에서 반품 신청 버튼을 표시한다.

프론트에서 확인할 것:

- 반품 상세 사유 10자 이상 검증
- 반품 요청 후 주문 상태가 `RETURN_REQUESTED` 또는 `반품 요청 접수`로 보이는지
- 이미 반품 요청한 주문에서 중복 신청을 막는지

### 7순위. 고객센터와 정책 링크

먼저 결정할 항목:

- 프론트 고정값 유지
- 설정 API 또는 관리자 관리값으로 분리

후보 필드:

- `support_phone`
- `support_email`
- `support_hours`
- `terms_url`
- `privacy_url`

MVP에서는 현재처럼 프론트 고정값으로도 충분하다.

## 9. 발표 또는 시연 전 체크리스트

- 데스크톱 첫 화면 확인
- 모바일 첫 화면 확인
- 홈 상품 클릭 시 상품 상세 이동 확인
- 상품 상세에서 예약함 담기 확인
- 예약함 수량 변경/선택 삭제 확인
- 예약 확인 → 주문서 작성 → 결제 → 주문 완료 흐름 확인
- 주문 상세 상태 타임라인 확인
- 반품 신청 → 반품 요청 완료 흐름 확인
- 로그인, 회원가입, 이메일 인증 이동 확인
- 마이페이지 진입 확인
- 배송지 관리/추가/수정 확인
- 회원 정보 보기/수정 확인
- 모바일 overflow 경고 확인
- `dart analyze` 통과 확인
